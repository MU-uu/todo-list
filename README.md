# 我的待办清单

一个基于四象限法(Eisenhower Matrix)的待办事项管理 Web 应用,纯前端实现,数据保存在浏览器 localStorage。

## ✨ 功能特性

- **四象限优先级管理**:重要·紧急 / 重要·不紧急 / 紧急·不重要 / 不重要·不紧急
- **视图切换**:列表视图 ↔ 四象限矩阵视图
- **筛选**:全部 / 未完成 / 已完成(两个视图同步)
- **截止时间 + 提醒**:设置年月日时分,自定义提前提醒时长(0/5/15/30 分钟,1/2 小时,1 天),系统通知 + UI 标签
- **编辑任务**:双击文字即可编辑,Enter 保存 / Esc 取消
- **排序**:每个任务配 ↑↓ 按钮,首末项自动禁用
- **优先级修改**:点击 ⚑ 旗标即可改优先级
- **拖动友好**:完整响应式(1200 / 900 / 600px 三档断点)
- **本地存储**:数据写入 `localStorage`,刷新不丢

## 🚀 使用方法

1. 打开 `index.html`(或访问部署后的 URL)
2. 输入待办内容,按 **Enter**
3. 弹窗选择优先级(四象限之一)
4. 弹窗选择是否设置截止时间
   - 选"设置" → 配置时间 + 提醒
   - 选"跳过" → 直接创建
5. 在列表/矩阵视图里勾选完成、编辑、排序、删除

## 🛠 技术栈

- 纯 HTML + CSS + JavaScript
- 无框架、无依赖、无后端
- 单文件应用(所有代码在 `index.html` 内)

## 📦 部署

支持任何静态网站托管服务(GitHub Pages、Vercel、Netlify 等),直接上传 `index.html` 即可。

**当前主部署**:**https://mu-todo-list.netlify.app**

## 📂 项目结构

```
.
├── index.html      # 主页面(部署入口)
├── todo.html       # 原始文件副本(保留)
├── README.md
└── .gitignore
```

## 🔒 数据隐私

所有数据存储在浏览器本地,不上传任何服务器。清空浏览器缓存 = 数据丢失,请定期备份重要待办。
